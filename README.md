# Lay Language AI

Tools and experiments for translating technical language into clearer, more accessible explanations.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -e ".[dev]"
```

## Test

```bash
pytest
```

